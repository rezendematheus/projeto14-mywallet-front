import styled from 'styled-components'

const StyledTitle = styled.div`
    height: 50px;

    font-family: 'sans-serif', Saira Stencil One;
    font-size: 32px;
    font-weight: 400;
    color: white;

    margin-bottom: 24px;
`

export default StyledTitle